<!DOCTYPE html>
<html>
  <head>
    <title>OtcDrug</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_pack_tag "application", media: "all", "data-turbolinks-track": "reload" %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <% if user_signed_in? %>
   <%= link_to "新規情報投稿", new_user_path %> 
   <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
   <% else %>
    <h3>現在ログインされてません。恐れいりますが、ログインお願いします</h3>
    <%= link_to "ログイン", new_user_session_path %>
    <%= link_to "新規登録", new_user_registration_path %>
    <% end %>
   <% flash.each do |flash_type, msg| %>
    <p style="color: <%= flash_type == 'notice' ? 'green' : 'red' %>;"><%= msg %></p>
    <hr>
    <% end %>
    <%= yield %>
    <div class="p-3 mb-2 bg-info text-white">©お薬ご提案アプリ2020 powered by koichi hatano</div>
  </body>
</html>
